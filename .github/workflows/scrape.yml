name: daily-update
on:
  schedule:
    - cron: '0 6 * * *'
  workflow_dispatch:

jobs:
  scrape-job:
    runs-on: ubuntu-latest
    permissions: { contents: write }
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - uses: actions/setup-python@v5
        with: { python-version: '3.11' }
      
      - name: Install dependencies
        run: pip install requests django djangorestframework gunicorn whitenoise

      - name: Scrape Ratings
        run: python -m players.update_ratings
        working-directory: backend 
        env:
          PYTHONPATH: .

      - name: Commit and Push to GitHub
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add backend/players/players.csv
          git commit --allow-empty -m "Automated update: $(date -u)"
          git push

      - name: Sync to Hugging Face
        env:
          HF_TOKEN: ${{ secrets.HF_TOKEN }}
        run: |
          git remote add hf https://redcomp16:$HF_TOKEN@huggingface.co/spaces/redcomp16/checkchesser-backend || git remote set-url hf https://redcomp16:$HF_TOKEN@huggingface.co/spaces/redcomp16/checkchesser-backend
          
          # Force push ensures Hugging Face picks up the new 'heartbeat' commit
          git push hf main --force
